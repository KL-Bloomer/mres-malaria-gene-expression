#!/bin/bash

snakemake -R heatmap_and_clustering --use-conda --printshellcmds --jobs 1 \
--config n_clst=8 ss=$PWD/sample_sheet.tsv datadir=/export/home/2592613b/mres-malaria-gene-expression \
--directory output \
--snakefile Snakefile \
 

 
 
